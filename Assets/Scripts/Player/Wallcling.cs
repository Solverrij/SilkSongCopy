using UnityEngine;

public class Wallcling : MonoBehaviour
{
    [SerializeField] private float clingDuration = 2.0f; // Duration the player can cling to the wall
    private float clingTimer;
    private bool isClinging = false;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
